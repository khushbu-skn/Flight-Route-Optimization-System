module.exports = {
  reactStrictMode: true,
  images: {
    domains: [],
  },
  // Output as a static site
  output: 'export',
  // Optional: Add a trailing slash to all paths
  trailingSlash: true,
  // Optional: Configure the output directory
  distDir: 'out',
}
